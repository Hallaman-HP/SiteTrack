; SiteTrack API — raise memory limit above the default 128M
; The /api/store response can materialise many asset_photos.photo_url data URLs
; (base64) in one PHP array; 128M OOMs. Raised to 512M as a safety net.
memory_limit = 512M
